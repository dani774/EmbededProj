import * as constants from './constants';

const defaultState = {
  testData: {},
};

const DataLabelingReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
